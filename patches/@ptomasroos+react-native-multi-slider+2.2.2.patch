diff --git a/node_modules/@ptomasroos/react-native-multi-slider/MultiSlider.js b/node_modules/@ptomasroos/react-native-multi-slider/MultiSlider.js
index f3114aa..524861d 100755
--- a/node_modules/@ptomasroos/react-native-multi-slider/MultiSlider.js
+++ b/node_modules/@ptomasroos/react-native-multi-slider/MultiSlider.js
@@ -45,6 +45,7 @@ export default class MultiSlider extends React.Component {
     snapped: false,
     vertical: false,
     minMarkerOverlapDistance: 0,
+    maxMarkerOverlapDistance: 0,
   };
 
   constructor(props) {
@@ -149,6 +150,10 @@ export default class MultiSlider extends React.Component {
       ? this.state.pastOne - accumDistance
       : accumDistance + this.state.pastOne;
     var bottom = 0;
+    if (this.props.maxMarkerOverlapDistance > 0) {
+      bottom = this.state.positionTwo - this.props.maxMarkerOverlapDistance;
+      if (bottom < 0) bottom = 0;
+    }
     var trueTop =
       this.state.positionTwo -
       (this.props.allowOverlap
@@ -224,6 +229,10 @@ export default class MultiSlider extends React.Component {
         ? this.props.minMarkerOverlapDistance
         : this.stepLength);
     var top = this.props.sliderLength;
+    if (this.props.maxMarkerOverlapDistance > 0) {
+      top = this.state.positionOne + this.props.maxMarkerOverlapDistance;
+      if (top > this.props.sliderLength) top = this.props.sliderLength;
+    }
     var confined =
       unconfined < bottom ? bottom : unconfined > top ? top : unconfined;
     var slipDisplacement = this.props.touchDimensions.slipDisplacement;
diff --git a/node_modules/@ptomasroos/react-native-multi-slider/index.d.ts b/node_modules/@ptomasroos/react-native-multi-slider/index.d.ts
index 678df51..7e4dfaf 100644
--- a/node_modules/@ptomasroos/react-native-multi-slider/index.d.ts
+++ b/node_modules/@ptomasroos/react-native-multi-slider/index.d.ts
@@ -72,6 +72,7 @@ export interface MultiSliderProps {
     markerOffsetX?: number;
     markerOffsetY?: number;
     minMarkerOverlapDistance?: number;
+    maxMarkerOverlapDistance?: number;
     imageBackgroundSource?: string;
     enableLabel?: boolean;
     vertical?: boolean;
